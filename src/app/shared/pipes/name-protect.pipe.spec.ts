import { NameProtectPipe } from './name-protect.pipe';

describe('NameProtectPipe', () => {
  it('create an instance', () => {
    const pipe = new NameProtectPipe();
    expect(pipe).toBeTruthy();
  });
});
