import { TouchAndMoveDirective } from './touch-and-move.directive';

describe('TouchAndMoveDirective', () => {
  it('should create an instance', () => {
    const directive = new TouchAndMoveDirective();
    expect(directive).toBeTruthy();
  });
});
