import { KeepAspectRatioDirective } from './keep-aspect-ratio.directive';

describe('KeepAspectRatioDirective', () => {
  it('should create an instance', () => {
    const directive = new KeepAspectRatioDirective();
    expect(directive).toBeTruthy();
  });
});
