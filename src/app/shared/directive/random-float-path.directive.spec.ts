import { RandomFloatPathDirective } from './random-float-path.directive';

describe('RandomFloatPathDirective', () => {
  it('should create an instance', () => {
    const directive = new RandomFloatPathDirective();
    expect(directive).toBeTruthy();
  });
});
